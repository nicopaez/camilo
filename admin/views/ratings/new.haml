%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:ratings, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:ratings, :new)

.tabs-content
  -form_for :rating, url(:ratings, :create), :class => 'form-horizontal' do |f|
    =partial 'ratings/form', :locals => { :f => f }
